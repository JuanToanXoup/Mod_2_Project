<h1> All Users </h1>
<% @users.each do |user| %>
    <% if user != current_user %>
        <div style="width:100px;"><image src="<%=(url_for(user.avatar))%>" style="max-width:100%;" /></div><%= link_to user.name, user_path(user) %><br>
    <%end%>
<% end %>