<% flash[:id] = @post.id %>

<h2><%= @post.title %></h2>
<ul>

<br><li><b><%= link_to "#{@post.trail.name}", trail_path(@post.trail_id) %></b></br>
<br><li><b><%= @post.meetup_date %></b></br>
<br><li><b><%= @post.description %></b></br>
<br>
<ul><br><% @post.post_comments.each do |p| %>
    <li><%=p.user.name%><ul><%= p.description %></li><br></ul>
    <%end%>
<br></ul>
<br><%= link_to "Comment on this Post!", new_post_comment_path, data: { turbolinks: false } %></br>
<%= button_to "Delete Post", {:controller => :posts, :action => 'destroy', :id => @post.id, :method => :delete} %>
<br><%= link_to "Posts", posts_path %></br>
</ul>
